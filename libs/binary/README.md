# binary

This library was generated with [Nx](https://nx.dev).

## Building

Run `nx build binary` to build the library.

## Running unit tests

Run `nx test binary` to execute the unit tests via [Vitest](https://vitest.dev/).
