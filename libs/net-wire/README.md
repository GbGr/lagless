# net-wire

This library was generated with [Nx](https://nx.dev).

## Building

Run `nx build net-wire` to build the library.

## Running unit tests

Run `nx test net-wire` to execute the unit tests via [Vitest](https://vitest.dev/).
