У меня есть ключевые поинты интересного нам метода по генерации 2d карт: @docs/2d-map-spec/survev-map-generation-spec.md
Так же есть исходный код проекта где используется этот метод генерации карт: /Users/pavlik228/survev

Нам нужно разработать униварсальный генератор 2d карт с использованием этого метода. Чтоб были объекты и фрагменты с коллизией и без, чтоб было API для
расширения/изменения функционала генератора.

Давай начнём с четкого brief'а по тому, что мы хотим получить в итоге.

Генератор который мы делаем должен быть адаптирован под наш фреймворк, часть симуляции должна быть детерминированой, render-часть - не обязательно.
Я думаю что должно быть 2 основных lib : libs/2d-map/2d-map-generator - генерация карт, libs/2d-map/2d-map-renderer - рендеринг карт.

Я могу где-то ошибаться, мне нужно чтоб ты помог мне исправить то что в моём плане ошибочно или предложить более лучшие альтернативы.

Результатом должен быть генератор 2d карт, по описанному методу, с очень высокой производительностью как на стороне симуляции так и на стороне render'а. И с API для расширения функционала генератора.

Результатом нашей с тобой работы должна стать полноценная спецификация / техническое задание на разработку генератора 2d карт, с описанием архитектуры, API и примерами использования.

так же я думаю что было бы неплохо чтоб у нас был API фич генерации то есть класс/модуль который отвечает за генерацию конкретныой фичи (рек, пляжей, травы и тд) чтоб можно было
добавлять новую фичу или просто при использовании генератора сообжать ему нужной набор фич для генерации карты.
